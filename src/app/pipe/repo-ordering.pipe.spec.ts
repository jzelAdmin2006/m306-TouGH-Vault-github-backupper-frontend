import { RepoOrderingPipe } from './repo-ordering.pipe';

describe('RepoOrderingPipe', () => {
  it('create an instance', () => {
    const pipe = new RepoOrderingPipe();
    expect(pipe).toBeTruthy();
  });
});
