import { ProtectionStatePipe } from './protection-state.pipe';

describe('ProtectionStatePipe', () => {
  it('create an instance', () => {
    const pipe = new ProtectionStatePipe();
    expect(pipe).toBeTruthy();
  });
});
